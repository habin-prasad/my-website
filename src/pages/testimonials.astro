---
import Base from '../layouts/Base.astro';

const testimonials = [
  {
    quote: "Habin consistently delivers clean, high-performance web systems. His focus on simplicity and architecture is top-tier.",
    author: "Jane Doe",
    role: "Engineering Lead",
    company: "TechCorp"
  },
  {
    quote: "One of the most pragmatic developers I've worked with. Excellent problem-solving skills across cloud infrastructure.",
    author: "John Smith",
    role: "Senior Staff Engineer",
    company: "DevCloud"
  }
];
---

<Base title="Testimonials">
  <section class="page-header">
    <h1>Testimonials</h1>
    <p class="subtitle">Kind words from colleagues, leaders, and peers I've collaborated with.</p>
  </section>

  <div class="testimonials-list">
    {testimonials.map(item => (
      <figure class="glass-card testimonial-card">
        <blockquote>"{item.quote}"</blockquote>
        <figcaption>
          <strong>{item.author}</strong>
          <span class="role">{item.role} at {item.company}</span>
        </figcaption>
      </figure>
    ))}
  </div>
</Base>

<style>
  .page-header { margin-bottom: 2rem; }
  .page-header h1 { font-size: 2.25rem; font-weight: 800; margin: 0 0 0.5rem 0; }
  .subtitle { color: var(--muted); font-size: 1.05rem; margin: 0; }

  .testimonials-list {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .testimonial-card {
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  blockquote {
    margin: 0;
    font-size: 1.05rem;
    line-height: 1.6;
    color: var(--text);
    font-style: italic;
  }

  figcaption {
    display: flex;
    flex-direction: column;
    font-size: 0.875rem;
    border-top: 1px solid var(--surface-border);
    padding-top: 0.75rem;
  }

  figcaption strong { color: var(--text); font-weight: 600; }
  .role { color: var(--muted); }
</style>